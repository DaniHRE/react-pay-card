import * as dino from '../assets/card-type/dino.png';

const BACKGROUNDS = [
    dino,
]

export const data = console.log(dino);

export function shuffleBackground() {
    return (
        data
    );
};
